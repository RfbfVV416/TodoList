version: '3'

services:
  pg_db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_PASSWORD=2812tyHx_8
      - POSTGRES_USER=postgres
      - POSTGRES_DB=postgres
      - PGDATA=/var/lib/docker/volumes/todolist_pg_project/_data
    volumes:
      - C:\Users\bebes\IdeaProjects\TodoList\pg_project:/var/lib/docker/volumes/todolist_pg_project/_data
    ports:
      - "5432:5432"
  pgadmin:
    container_name: pgadmin4_container
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: bebeshkomail@mail.ru
      PGADMIN_DEFAULT_PASSWORD: 2812tyHx_8
    ports:
      - "5555:80"
  my-springboot-container:
    image: proj-docker-compose:1
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8080:8080"


volumes:
  pg_project:
    driver: local
